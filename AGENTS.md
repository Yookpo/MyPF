# MyPF Codex 작업 지침

## 프로젝트 목표

DirectX 11 기반의 1~2분 분량 실시간 사이버펑크 골목 렌더링 데모를 제작한다.

최종 목표 기능:

- 1인칭 카메라
- WASD 이동과 마우스 시점
- 네온 간판과 다수의 점광원
- 젖은 바닥 반사
- 그림자
- Bloom
- 안개와 분위기 표현
- 색조 보정
- ImGui 디버그 UI
- 전원 스위치 상호작용
- 조명의 순차 점등 연출

## Codex와 사용자 역할

- 모든 C++와 HLSL 코드는 사용자가 직접 작성한다.
- Codex는 사용자의 명시적인 요청 없이 프로젝트 소스 코드를 생성하거나 수정하지 않는다.
- Codex는 필요한 클래스, 데이터, 메서드 이름, 책임과 구현 순서를 설명한다.
- 한 번에 작은 기능 하나씩 진행한다.
- 사용자가 수정을 완료하면 현재 작업 트리를 다시 읽고 코드 리뷰를 수행한다.
- 오류가 발견돼도 Codex가 자동으로 수정하지 않고 원인과 수정 방향을 설명한다.
- 필요한 경우 Debug x64 빌드와 짧은 실행으로 검증한다.

## 아키텍처 원칙

- 하위 시스템은 상위 시스템을 알지 않는다.
- Renderer는 Scene, GameObject, Transform을 포함하거나 참조하지 않는다.
- Scene과 GameObject는 DirectX 11 및 ImGui를 알지 않는다.
- Transform은 GPU 또는 HLSL의 행렬 저장 규칙을 알지 않는다.
- 상위 Render Submission 계층이 Scene 데이터를 Renderer용 데이터로 변환한다.
- Renderer는 Mesh, Material, Matrix 같은 저수준 렌더링 데이터만 받는다.
- 리소스의 소유권과 비소유 참조를 명확히 구분한다.
- 불필요하게 범용적인 ECS나 추상화 시스템을 먼저 만들지 않는다.

## 작업 방식

1. 작업 전 현재 소스와 Git 상태를 확인한다.
2. 현재 단계의 목적과 완료 조건을 먼저 설명한다.
3. 사용자가 직접 구현한다.
4. Codex가 수정 결과를 리뷰한다.
5. 빌드와 실행으로 검증한다.
6. 검증이 끝난 뒤 다음 기능으로 넘어간다.

## 빌드 정보

- 저장소 루트: `MyPF`
- 솔루션: `MyPF/MyPF.sln`
- 기본 검증 구성: `Debug | x64`
- HLSL 파일은 `MyPF/Shaders`에 있다.
- 실행 시 Shader 상대 경로가 올바르게 해석되도록 작업 디렉터리를 확인한다.

## 변경 안전성

- 사용자가 작성한 기존 변경사항을 보존한다.
- 요청받지 않은 파일을 수정하거나 삭제하지 않는다.
- `.vs`, `x64`, `CodexBuild` 등의 빌드 산출물을 커밋하지 않는다.
- 사용자가 요청하지 않는 한 Git commit, push, branch 변경을 수행하지 않는다.